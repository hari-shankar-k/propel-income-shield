package com.guide_wire.demo.enums;

public enum PolicyStatus {

    ACTIVE,      // 🟢 Policy is currently valid
    EXPIRED,     // ⏳ Policy duration ended
    CANCELLED    // ❌ User cancelled the policy
}